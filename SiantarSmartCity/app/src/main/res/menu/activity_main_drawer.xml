<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_camara" android:icon="@android:drawable/ic_menu_agenda"
            android:title="Semua Pengaduan">@color/colorPrimary</item>
        <item android:id="@+id/nav_gallery" android:icon="@android:drawable/ic_menu_info_details"
            android:title="Pengaduan Saya" />
    </group>

    <item android:title="Settings">
        <menu>
            <item android:id="@+id/nav_logout" android:icon="@android:drawable/ic_menu_upload"
                android:title="Keluar" />
        </menu>
    </item>

</menu>